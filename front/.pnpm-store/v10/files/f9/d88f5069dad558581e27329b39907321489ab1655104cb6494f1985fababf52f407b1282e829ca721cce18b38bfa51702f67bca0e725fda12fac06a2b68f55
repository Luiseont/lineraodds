import type { Chain } from '../../chain';
declare global {
    interface WalletAccountMap {
    }
}
export type BaseWalletAccount<TChain extends Chain = Chain> = {
    address: string;
    chain: TChain;
    id: string;
    lastSelectedAt: Date | null;
    verifiedCredentialId: string | null;
    walletProviderKey: string;
};
export type WalletAccount = WalletAccountMap[keyof WalletAccountMap] extends never ? BaseWalletAccount<Chain> : WalletAccountMap[keyof WalletAccountMap];
//# sourceMappingURL=walletAccount.types.d.ts.map